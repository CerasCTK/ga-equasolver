{
    "name": "ga-equasolver",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "format": "bun x prettier . --write",
        "test": "vitest run"
    },
    "devDependencies": {
        "prettier": "^3.9.6",
        "vite": "^8.1.1",
        "vitest": "^3.0.0"
    },
    "prettier": {
        "semi": true,
        "tabWidth": 4,
        "singleQuote": false
    }
}
